{
  "name": "selador-apps-homepage",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.31.0",
    "@vitejs/plugin-react": "^5.1.2",
    "gh-pages": "^6.1.1",
    "vite": "^7.3.0",
    "wrangler": "^4.80.0"
  }
}
